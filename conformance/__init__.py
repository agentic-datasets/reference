"""Repository-side conformance configuration: which subjects to test, and the
generator that emits the normative data from the reference fixtures.

The harness itself is the separately distributed `agentic-dataset-conformance`
package, which imports nothing from here.
"""
